export const minWidth = {
  large: '128px',
  base: '96px',
  small: '64px',
};

export type MinWidthType = typeof minWidth;
export type KeyMinWidth = keyof MinWidthType;

export const padding = {
  large: '8px 12px',
  base: '6px 12px',
  small: '4px 8px',
};

export type PaddingType = typeof padding;
export type KeyPadding = keyof PaddingType;
